# Core
faster-whisper>=1.0.0
pyannote.audio>=3.1.0
torch>=2.0.0
numpy>=1.24.0

# GUI
PySide6>=6.6

# Audio recording: microphone (shared WASAPI capture) and system-audio
# loopback capture. Neither the bundled ffmpeg build nor QtMultimedia's
# QAudioSource can do either on Windows (confirmed by direct testing) -
# sounddevice handles the microphone, soundcard handles system-audio
# loopback (sounddevice's installed build has no loopback support at all -
# no such parameter exists on its WasapiSettings).
sounddevice>=0.4.6
soundcard>=0.4.3
scipy>=1.10.0

# Recording-in-progress taskbar badge: Qt6 dropped QtWinExtras, which used
# to wrap Windows' ITaskbarList3::SetOverlayIcon - comtypes is the plain,
# pure-Python way left to call that COM API directly (confirmed by direct
# testing).
comtypes>=1.2.0

# pyannote deps
omegaconf>=2.3.0
huggingface_hub>=0.20.0

# NOTE: torch above installs CPU-only build.
# For NVIDIA GPU (CUDA 12.6):
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
# For CUDA 11.8:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
