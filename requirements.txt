# Core
faster-whisper>=1.0.0
pyannote.audio>=3.1.0
torch>=2.0.0
numpy>=1.24.0

# GUI
PySide6>=6.6

# Audio recording: microphone (shared + WASAPI exclusive mode) and
# system-audio loopback capture. Neither the bundled ffmpeg build nor
# QtMultimedia's QAudioSource can do either on Windows (confirmed by direct
# testing) - sounddevice handles the microphone (its WasapiSettings(exclusive=)
# is the only one of the two that reliably supports exclusive mode; soundcard's
# own exclusive_mode raises "invalid argument" on real hardware), soundcard
# handles system-audio loopback (sounddevice's installed build has no loopback
# support at all - no such parameter exists on its WasapiSettings).
sounddevice>=0.4.6
soundcard>=0.4.3

# pyannote deps
omegaconf>=2.3.0
huggingface_hub>=0.20.0

# NOTE: torch above installs CPU-only build.
# For NVIDIA GPU (CUDA 12.6):
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
# For CUDA 11.8:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
